# DeepCAD Professional CAE System 依赖列表
# 核心依赖 (必需)
PyQt6>=6.4.0
numpy>=1.21.0
pandas>=1.3.0

# 3D可视化 (推荐)
pyvista>=0.38.0
pyvistaqt>=0.9.0

# 图表绘制 (推荐)
matplotlib>=3.5.0

# 界面图标 (推荐)
qtawesome>=1.2.0

# 地质建模 (可选)
gempy>=3.0.0

# 科学计算扩展 (可选)
scipy>=1.7.0
scikit-learn>=1.0.0

# 系统监控 (可选)
psutil>=5.8.0

# 数据处理扩展 (可选)
openpyxl>=3.0.0  # Excel文件支持
h5py>=3.0.0      # HDF5文件支持

# 网络功能 (可选)
requests>=2.25.0

# 开发工具 (开发时使用)
pytest>=6.0.0
black>=22.0.0
flake8>=4.0.0
