import React from 'react'
import ReactDOM from 'react-dom/client'
import { FEAWorkspace } from './components/FEAWorkspace'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FEAWorkspace />
  </React.StrictMode>,
)